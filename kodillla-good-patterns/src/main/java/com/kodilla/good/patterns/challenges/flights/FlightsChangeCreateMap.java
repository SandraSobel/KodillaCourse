package com.kodilla.good.patterns.challenges.flights;

import java.util.HashMap;
import java.util.List;
import java.util.stream.Collectors;

public class FlightsChangeCreateMap {
  /*  FlightsDatabase flightsDatabase = new FlightsDatabase();
    public HashMap<Flight,Flight> createHashMap() {
        HashMap<Flight,Flight> flightsWithChange = new HashMap<>();
        Flight flightTo = new Flight();
        Flight flightFrom = new Flight();
        flightsDatabase.flightsList();
        for (int i = 1; i < flightsDatabase.flightsList().size();i++){

        }




        return flightsWithChange;
    }*/
}
